# 本地开发（npm run dev）无条件走 API，无需 JSON 兜底
# 以下变量在生产 gh-pages fallback 或正式接后端时使用
VITE_USE_API=true
VITE_API_BASE_URL=/api
VITE_APP_BASE=/aw-demo/

# gh-pages 静态部署才启用 JSON fallback
VITE_USE_LOCAL_JSON_FALLBACK=false